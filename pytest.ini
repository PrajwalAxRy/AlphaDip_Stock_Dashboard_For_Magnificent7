[pytest]
testpaths = tests
markers =
    unit: Unit tests that run without external services.
    integration: Integration tests that may use real infrastructure.
    e2e: End-to-end tests for user flows.
